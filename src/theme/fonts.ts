const fontName = 'OpenSans';

const fonts = {
  bold: `${fontName}-Bold`,
  extraBold: `${fontName}-ExtraBold`,
  light: `${fontName}-Light`,
  medium: `${fontName}-Medium`,
  regular: `${fontName}-Regular`,
  semiBold: `${fontName}-SemiBold`,
};

export {fonts};
